requests>=2.31.0
pyyaml>=6.0.1
pynacl>=1.5.0
gitpython>=3.1.41
python-bitcoinrpc>=1.0
# yescrypt Python binding not available on PyPI — validate_shares.py handles
# import failure gracefully via try/except; SHA256d path works without it.
